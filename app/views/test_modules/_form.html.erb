<%= form_for(@test_module) do |f| %>
  <% if @test_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_module.errors.count, "error") %> prohibited this test_module from being saved:</h2>

      <ul>
      <% @test_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
