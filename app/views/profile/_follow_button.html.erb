<% if session[:user_id] != user_info.id %>

	<% if unfollow == 1 %>
		<%= button_to 'Unfollow', {controller: 'profile', action: 'follow', :user_to_follow => user_info.id}, :method => :post, :class => 'vigme-button btn btn-primary' %>

	<% else %>
		<%= button_to 'Follow', {controller: 'profile', action: 'follow', :user_to_follow => user_info.id}, :method => :post, :class => 'vigme-button btn btn-primary' %>
	<% end %>
<% end %>